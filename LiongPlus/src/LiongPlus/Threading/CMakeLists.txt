report_using(USE_THREADING "LiongPlus::Threading")
if (USE_THREADING)
	set(LIONGPLUS_SOURCES ${LIONGPLUS_SOURCES}
		${CMAKE_CURRENT_SOURCE_DIR}/Dispatcher.cpp
	)
endif()
set (LIONGPLUS_SOURCES ${LIONGPLUS_SOURCES} PARENT_SCOPE)
